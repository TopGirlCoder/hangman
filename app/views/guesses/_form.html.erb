<%= form_for([user, game, Guess.new], remote: true) do |f| %>
  <% if guess.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(guess.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% guess.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :letter %>
    <%= f.text_field :letter %>
  </div> 

  <div class="actions">
    <%= f.submit value:"Try Letter!" %>
  </div>
<% end %>